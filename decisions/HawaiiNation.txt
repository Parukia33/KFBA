country_decisions = {

	unite_hawaii = {
		major = yes
		potential = {
			NOT = { tag = HLR }
			NOT = { tag = PAP }
			NOT = { tag = UHW }
			primary_culture = polynesian
			capital_scope = {
				region = hawaii_region
				is_core = ROOT
			}
			hawaii_region = {
				owned_by = ROOT
			}
			OR = {
				ai = no
				AND = {
					ai = yes
					num_of_cities = 2	
				}
			}
		}
		provinces_to_highlight = {
			region = hawaii_region
			NOT = { owned_by = ROOT }
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			capital_scope = {
				region = hawaii_region
				is_core = ROOT
			}
			hawaii_region = {
				type = all
				owned_by = ROOT
				is_core = ROOT
				is_state = yes
			}
			NOT = { exists = UHW }
		}
		effect = {
			change_tag = UHW
			on_change_tag_effect = yes
			if = {
				limit = {
					NOT = { government_rank = 2 }
				}
				set_government_rank = 2
			}
			if = {
				limit = {
					has_custom_ideas = no
					NOT = { has_idea_group = hawaiian_ideas }
				}
				country_event = { id = ideagroups.1 } #Swap Ideas
			}
			add_country_modifier = {
				name = "centralization_modifier"
				duration = 7300
			}
			add_prestige = 25
		}
		ai_will_do = {
			factor = 1
		}
	}
}
